<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codelog.secury">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme"
        android:fitsSystemWindows="true"
        android:usesCleartextTraffic="true">

        <meta-data android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="true" />

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".atelier.AtelierActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".code.NeedCodeActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".code.NewCodeActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".setting.DynamiteActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".setting.SettingActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".setting.ASettingActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".setting.StartWebsiteActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".SBoxActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".WebActivity"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>